import { APP_NAME } from '@resume-roast/shared';

/**
 * Temporary placeholder proving the API workspace resolves the shared package.
 * Replaced by the real Express server (health endpoints, logging) in Step 3/4.
 */
export const apiServiceName = `${APP_NAME} — API`;
