# Web app environment configuration.
# Copy to `.env` (git-ignored) and adjust locally. Never commit real secrets.

# Base URL of the API. NEXT_PUBLIC_ vars are exposed to the browser, so never
# place secrets here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
