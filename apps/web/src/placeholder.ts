import { APP_NAME } from '@resume-roast/shared';

/**
 * Temporary placeholder proving the web workspace resolves the shared package.
 * Replaced by the real Next.js App Router app in Story 1, Step 3.
 */
export const webAppTitle = `${APP_NAME} — Web`;
