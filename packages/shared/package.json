{
  "name": "@resume-roast/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types and utilities for the AI Resume Roast Generator.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@resume-roast/config": "workspace:*",
    "typescript": "6.0.3"
  }
}
